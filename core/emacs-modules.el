;;; emacs-modules.el --- Emacs modules -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:
(defcustom emacs-core-modules '()
  "Emacs enabled core modules."
  :group 'emacs-core
  :type '(repeat symbol))

(defcustom emacs-modules '()
  "Emacs enabled modules."
  :group 'emacs-core
  :type '(repeat symbol))
;;; emacs-modules.el ends here.
